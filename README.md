# Texas Furniture Showcase

A static gallery website for luxury furniture (wardrobes, tables, chairs).

## Usage

Open `index.html` in a browser, or serve with:

```bash
python3 -m http.server 8000
```

All furniture images are bundled in the repo root.
